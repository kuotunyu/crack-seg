[tool.ruff]
target-version = "py39"
line-length = 120
extend-exclude = ["colab_train.ipynb"]

[tool.ruff.lint]
# 預設規則（pyflakes F + pycodestyle 錯誤子集）已足夠保守，
# 另加 I（import 排序）維持一致風格
select = ["E4", "E7", "E9", "F", "I"]
